services:
  inventory-service:
    image: base
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
      - ../shared:/shared
    networks:
      - sharednet

networks:
  sharednet:
    name: shared-network
    external: true

