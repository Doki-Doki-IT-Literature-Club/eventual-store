services:
  inventory-service:
    image: base
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
    networks:
      - sharednet

networks:
  sharednet:
    name: shared-network
    external: true

